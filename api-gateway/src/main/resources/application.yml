server:
  port: 8081

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: ticket-service-route
          uri: http://localhost:8080
          predicates:
            - Path=/api/tickets/**